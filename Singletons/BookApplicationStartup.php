<?php

require_once 'BookApplication.php'; // Include the file containing the BookApplication class

// Simulating the behavior of Java's BookApplicationStartup main method
function main() {
    BookApplication::getInstance();
}

// Call the main function to start the application
main();

